# ROS 2 Packages

## What I learned

A ROS 2 package is used to organize ROS 2 code and resources.

## Commands I learned

```bash
ros2 pkg create my_robot
colcon build
ros2 pkg list

My first package

I created a ROS 2 package called my_robot.

What I understood
A ROS 2 workspace can contain multiple packages.
A package organizes ROS 2 code and resources.
colcon build is used to build the workspace.
ros2 pkg list shows available ROS 2 packages.
Practice

Today I created my first ROS 2 package and learned the basic ROS 2 workspace and package workflow.


